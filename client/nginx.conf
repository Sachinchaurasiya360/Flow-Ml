server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # API is called directly by the client at https://api.visualml.xyz - no proxy needed here.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
